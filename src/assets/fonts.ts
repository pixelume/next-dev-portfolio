import { Inter, Quicksand, Reenie_Beanie } from 'next/font/google'

export const inter = Inter({ subsets: ['latin'] })
export const quicksand = Quicksand({ subsets: ['latin'], weight: ['300'] });
export const reenieBeanie = Reenie_Beanie({ subsets: ['latin'], weight: ['400'] });
